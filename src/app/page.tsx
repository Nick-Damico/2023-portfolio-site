import Image from 'next/image'

export default function Home() {
  return (
    <main className="flex-col flex min-h-screen items-center justify-between bg-black">
      <div className="flex grow max-w-screen-lg w-full h-100 bg-white">
        Hello World
      </div>
    </main>
  )
}
